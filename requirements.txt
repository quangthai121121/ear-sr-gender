# Classifier / evaluation environment.
# NOTE: Real-ESRGAN and SwinIR have their OWN separate requirements
# (installed inside external/Real-ESRGAN and external/SwinIR by
# scripts/01_clone_sr_repos.sh) -- keep that env separate if you hit
# dependency conflicts (e.g. basicsr pinning old torchvision).

torch>=2.1
torchvision>=0.16
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
statsmodels>=0.14
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.66
matplotlib>=3.7
tabulate>=0.9        # needed for DataFrame.to_markdown()
